[project]
name = "tests"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "pytest-embedded>=2.7.0",
    "pytest-embedded-arduino>=2.7.0",
    "pytest-embedded-serial-esp>=2.7.0",
    "pytest-html>=4.2.0",
]
